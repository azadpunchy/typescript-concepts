// ctrl k i to see definaion of any variable or function or anything
let userName: string = "Jack";
let hasLoggedIn: boolean = true;

userName += " Sharma";

let myNumber: number = 10;

let regx: RegExp = /foo/;

const names: string[] = userName.split(" ");
console.log(names);

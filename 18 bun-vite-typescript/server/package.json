{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun --watch index.ts",
    "build": "bun build ./*.ts --outdir ./out --splitting --entry-naming [dir]/[name]-manan-[hash].[ext] --minify --sourcemap=external"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
